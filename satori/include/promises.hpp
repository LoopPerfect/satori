#ifndef SATORI_PROMISES_HPP
#define SATORI_PROMISES_HPP

#include <satori/promise.hpp>

// template <typename T>
// Promise<T> createPromise(Loop& loop) {
//
//   auto* actor = loop->newActor<T>();
//
//   // actor->push();
//
//   return Promise(actor);
//
// }

#endif
