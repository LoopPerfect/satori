# Examples

These are examples of how you can use Satori to perform a range of IO tasks.

Each example can be run using [Buck](https://www.buckbuild.com):

```
buck run examples/cat
buck run examples/ls
buck run examples/pwd
# etc...
```
